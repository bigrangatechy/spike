# Package lists

- `spike-live.list.chroot` — Stage 1 live + admin packages + default apps (Firefox/Thunderbird/VLC/LibreOffice).

Mozilla APT for Firefox/Thunderbird: see `config/spike-archives/` (not Ubuntu Snap stubs).

Do not split Standard/Plus into separate list files for separate ISOs.
