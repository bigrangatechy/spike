# live-build `auto/`

Scripts/config here will eventually drive `lb config` (classic live-build `auto/config`, `auto/clean`, `auto/build` pattern).

Until filled:

- Do not expect `lb config` to succeed from an empty tree.
- Prefer invoking builds only through `scripts/build-iso.sh` so checks stay consistent.

One ISO only — no auto profiles for Standard vs Plus images.
