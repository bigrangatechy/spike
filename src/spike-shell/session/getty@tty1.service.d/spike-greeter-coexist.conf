# Soften getty teardown when spike-greeter Conflicts=getty@tty1.
# Default TTYVTDisallocate=yes can clear the framebuffer after the greeter
# has already painted (flash → blank UI while PAM still accepts input).
[Service]
TTYVTDisallocate=no
